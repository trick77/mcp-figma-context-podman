.env
.env.bak
*.local

# Operator-supplied corporate root CAs. Drop *.crt / *.pem into ./certs/.
# Never committed; the dir itself is kept via .gitkeep.
certs/*
!certs/.gitkeep

# Figma assets downloaded by the container at runtime. Bind-mounted to
# /home/node/images so host-side agents can read them directly. Never committed.
images/*
!images/.gitkeep

# IDEs / editors
.idea/
*.iml
.vscode/
.vs/
.fleet/
.zed/
.history/
*.swp
*.swo
.nvim.lua
.nvimrc

# OS
.DS_Store
Thumbs.db
